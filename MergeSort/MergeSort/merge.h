#pragma once
#include <stdio.h>
#include <stdlib.h>

void mergeSort(int arr[], const int _begin, const int _end);
void merge(int arr[], const int _begin, const int _end);
void printArr(int arr[], const int _length);